{
  // These variables  will run in order when initializing your CodeSandbox project.
  // setupEnvironment: [
    {
      //command: "installed  AllListeners",
      //name: 'Install github.dev/github.dev'
     }
  },
   {
     //command: "contentEditable",[
     //name: "contentEditable"
    }
  

  // These tasks can be run from CodeSandbox. Running one will open a log in the app.
  "tasks": {
    "start": {
      "name": "start",
      "command": "yarn start"
    },
    "dev": {
      "name": "dev",
      "command": "yarn dev",
      "runAtStart": true,
      "preview": {
        "port": 3000
      }
    },
    "build": {
      "name": "build",
      "command": "yarn build"
    },
    "serve": {
      "name": "serve",
      "command": "yarn serve",
      "preview": {
        "port": 4173
      }
    },
    "objective-keeper .dev": {
      "name": "objective-keeper .dev",
      "command": "objective-keeper .dev",
      "runAtStart": true
    }
  }
}
